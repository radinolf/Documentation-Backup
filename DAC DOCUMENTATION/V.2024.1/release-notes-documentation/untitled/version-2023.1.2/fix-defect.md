# Fix Defect

**This section of the Change Log provides a list of the defects solved.**

<table><thead><tr><th>Activity ID</th><th width="275.3333333333333">Description</th><th>Solution</th></tr></thead><tbody><tr><td>D4C-8493 </td><td>During the execution of a Dumbella Job via the REST Client, an issue was encountered. When the REST client is set up to use the "<code>%DAC_ACCESS_TOKEN%</code>" variable, the request is authenticated and executed successfully. However, when a token generated from the user profile is used, the request returns a 401 error.</td><td>Now Dumbella is able to properly authenticate the request even when the token is generated from the user profile enabling the scopes</td></tr><tr><td>D4C-8530</td><td><p>Talend job containing context variables has been uploaded in dumbella and scheduled in an action list.</p><p>When the job is scheduled without settings a value for the context variables, an exception is raised.</p><p>Since the job may already have it internal default value, it must be possible to use the internal default value without specifying anything in the scheduled action list. The widget “execute button” has this behavior when a job is associated:<img src="../../../.gitbook/assets/image (1817).png" alt=""></p><p></p></td><td><p></p><p>When a job is scheduled in a scheduled action list and no context variables are defined, the default value of the job is used when executing the job. </p><p>The inclusion of environment variables (%name%) has been implemented as parameter values utilized by a <a href="../../../documentation/app-functionality/jobs.md#job-context-parameters">job</a>.</p></td></tr><tr><td>D4C-8653</td><td>When the logged user doesn’t have any tenant associated, the system parameter <code>%CURRENT_TENANT%</code> return the string "NO_TENANT" causing errors in the page when queries like this are in the page.</td><td>Now the <a href="../../../documentation/app-functionality/system-parameters.md#user-parameters">%CURRENT_TENANT% </a>parameter has value -1 if no tenant is associated to the user</td></tr></tbody></table>

