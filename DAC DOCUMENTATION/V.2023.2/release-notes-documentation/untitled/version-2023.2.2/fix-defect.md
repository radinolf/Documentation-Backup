# Fix Defect

<table data-full-width="true"><thead><tr><th width="165">Activity ID</th><th width="499">Description</th><th>Solution</th></tr></thead><tbody><tr><td>D4C-8495</td><td>When you make a call from REST API Client and the response is a 401 in the state the value "0" is shown.<img src="../../../.gitbook/assets/image (174).png" alt=""></td><td>The problem has been solved. Now in the STATUS the response code is correctly reported.</td></tr><tr><td>D4C-8503</td><td>Hub Dialog API returns also unpublished widgets</td><td><p>The problem has been solved .</p><p>Only the list of published widgets will be returned in the api specified in the ticket.</p></td></tr><tr><td>D4C-8976</td><td>The an application developer select a column in the property depends-on-column in the column-definition of a report, then he can not removed anymore the column. If the user configure this property by mistake, then he will not be able anymore to restore it.</td><td><p>The problem has been solved.</p><p>Now the Application developer can remove the selected column.</p></td></tr><tr><td>D4C-9043</td><td>When a user creates/edits a datasource, it is not displayed in the list of datasources selectable by the SQL Report. The list is always empty. Only after reloading the application (closing and opening the DAC), the list is populated correctly.</td><td>The problem has been solved.</td></tr><tr><td>D4C-8730</td><td>PDF file exported from Smart Grid raise exceptions in some scenarios. </td><td>The problem has been solved.</td></tr><tr><td>D4C-9045</td><td>There are a couple of scenarios in which using external OIDC provider, the user creation fails and the error message is not easy to understand for the business user. <img src="../../../.gitbook/assets/image (178).png" alt=""></td><td><p>The problem has been solved.</p><p>Now the message has more user-friendly. <img src="../../../.gitbook/assets/image (177).png" alt=""></p></td></tr><tr><td>D4C-9046</td><td><p>Impossible to logout if user is disabled or exception is raised on user creation</p><p> <img src="../../../.gitbook/assets/image (196).png" alt=""></p></td><td>The problem was solved with <a href="migration-notes.md">Migration Impact</a></td></tr><tr><td>D4C-9113</td><td>Landing page doesn’t show anymore the labels of the folder</td><td>The problem has been solved.</td></tr><tr><td>D4C-9114</td><td>Drill-through "full screen" is not anymore in full screen</td><td><p>The problem has been solved.</p><p>Now the Drill-Through is in full screen.</p></td></tr><tr><td>D4C-9129</td><td>Loader not removed after saving the edit configuration of a KPI</td><td>The problem has been solved. Now when clicking on “Save”, the dialog stays open the but the loader icon is removed.</td></tr><tr><td>D4C-9118</td><td>Change folder description from landing page. If change the description of a folder from the landing page, this description will not be reported in next edits of the folder see attachment.</td><td>The problem has been solved</td></tr><tr><td>D4C-9127</td><td><p>The error dialog triggered from the Smart Grid widget are displayed behind the widget’s dialog rather than in front of the dialog itself.</p><p><img src="../../../.gitbook/assets/image (188).png" alt=""></p></td><td>The problem has been solved. Now the error dialog appear above the Smart Grid dialog.</td></tr><tr><td>D4C-9132</td><td>When deleting a message in the discussion widget, the UI has an infinite loader, and it isn't possible to remove the message. </td><td>The problem has been solved. Now it’s possible to remove a message from the discussion widget.</td></tr><tr><td>D4C-9134</td><td><p>User profile page doesn’t load when one of the following languages are used:</p><ul><li>TH</li><li>DK</li><li>LV</li><li>EG</li><li>ID</li></ul></td><td>The problem has been solved. Now it’s possible to use the User Profile page with any language</td></tr><tr><td>D4C-9143</td><td>Direct access link to page doesn't pass parameter value in query string</td><td>The problem has been solved. Now the direct access link to page , pass the value sin the query string.</td></tr><tr><td>D4C-9174</td><td>Business users can't access and read Terms of Use</td><td>The problem has been solved. Now the Business User can open the Terms of Use</td></tr><tr><td>D4C-9175</td><td>Access Denied creating a document for not Administrator user</td><td>The problem has been solved. Now Administrator  user can create a document.</td></tr><tr><td>D4C-9181</td><td>Administrator flag visible also for non-administrator user</td><td>The problem has been solved. Now any user that is not configured as administrator must not display the flag “administrator” on user editing.</td></tr><tr><td>D4C-9199</td><td>Error dialog with "LABEL_NOT_FOUND"<img src="../../../.gitbook/assets/image (198).png" alt="" data-size="original"></td><td>The problem has been solved. </td></tr><tr><td>D4C-9161</td><td>JS exception "Cannot read properties of null" raised again. <img src="../../../.gitbook/assets/image (199).png" alt="" data-size="original"></td><td>The problem has been solved. </td></tr><tr><td>D4C-9115</td><td>Exception "Something went wrong. To retry login clear application cookies" is raised when user try to login.<img src="../../../.gitbook/assets/image (200).png" alt=""></td><td>The problem has been solved. </td></tr></tbody></table>
