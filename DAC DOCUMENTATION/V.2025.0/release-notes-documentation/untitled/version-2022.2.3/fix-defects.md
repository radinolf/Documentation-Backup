# Fix Defects

**This section of the Change Log provides a list of the defects solved.**

<table><thead><tr><th width="131.33333333333331">Activity ID</th><th width="452">Description</th><th></th></tr></thead><tbody><tr><td>D4C-7985</td><td><p>An exception is thrown when we try to create or to synchronize in a target environment a new application.</p><p><img src="../../../.gitbook/assets/image (719).png" alt=""></p><p></p></td><td>Now it is possible to synchronize or create applications in the target environment.</td></tr><tr><td>D4C-8004</td><td><p>After the synchroniztion of a new application in a target enviroment, pages are not visible from web or from Design Studio.</p><p>After the synchronization, in the list of objects of the target environment, the item “<strong>Page Catalog</strong>” is not visible.</p></td><td>Now after the synchronization, pages are visible both in Design Studio and web</td></tr><tr><td>D4C-7873</td><td><p>The record displayed in a crosstab widgets shows HTML encoding rather than the real char. </p><p>Example:<img src="../../../.gitbook/assets/image (1601).png" alt="" data-size="original"></p></td><td>Now the record in a crosstab widget doesn't show HTML encoding.</td></tr><tr><td>D4C-7913</td><td>The headers of crosstab widgets shows HTML encoding rather than the real char:<img src="../../../.gitbook/assets/image (556).png" alt=""></td><td>Now headers of crosstab doesn’t show HTML encoding if chars with accent are used as level’s header OR in multilanguage tags.</td></tr><tr><td>D4C-7947</td><td>After a synchronization of widgets, in the target environment there are duplicated HUB folders with the same widget but not published.</td><td>The bug that was causing the duplication of the Widget Hub subfolder and widgets has now been fixed. If your environment was affected by the issue, please contact <a href="mailto:support@decisyon.com">support@decisyon.com</a></td></tr><tr><td>D4C-7951</td><td>It is not possible to crop new images used as logo in the "Logo&#x26;Colors" sections.</td><td>Now it is possible to crop the image in the Logo&#x26;Colors section.</td></tr><tr><td>D4C-7980</td><td>It is not possible to associate an image to a folder or a page in the web application.</td><td>Now it is possible to associate a picture to a folder or a page.</td></tr></tbody></table>
